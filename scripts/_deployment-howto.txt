1) deployment-01
    - will ge source from svn, create iswatch an qwatch version
2) deployment-02
    - will compile dynamic binaries for linux, see output, test it
3) deployment-03
    - will prepare windows build folders
4) go into windows, run dist/win-compile.bat
5) run deployment-04
6) compile nsi scripts


- change exe file summary info (manual step)